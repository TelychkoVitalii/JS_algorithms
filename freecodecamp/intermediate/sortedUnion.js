// function uniteUnique(arr) {
//     const args = Array.prototype.slice.call(arguments),
//           splitArr = args.reduce((a, b) => a.concat(b));
//     return splitArr.filter((num, index) => splitArr.indexOf(num) === index);
// }
//
// uniteUnique([1, 3, 2], [5, 2, 1, 4], [2, 1]);